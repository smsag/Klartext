# Ad-hoc backups made beside the live theme are never part of the release;
# git history is the backup now.
*.backup-*.css
*.backup-*.json
*.backup-*.md

# macOS and iCloud housekeeping
.DS_Store
*.icloud
